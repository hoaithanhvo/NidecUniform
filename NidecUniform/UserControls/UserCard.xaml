<UserControl x:Class="NidecUniform.UserControls.UserCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             Name="userCard">
    <Border Padding="15 10">
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="Background" Value="White"/>
                <Setter Property="Margin" Value="0 4"/>

                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#039F41"/>
                    </Trigger>

                    <DataTrigger Binding="{Binding ElementName=userCard,Path=IsActive}">
                        <Setter Property="Background" Value="#f3f3f3"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <!--User Image-->
            <Ellipse Width="50" Height="50"  VerticalAlignment="Center" HorizontalAlignment="Center">
                <Ellipse.Fill>
                    <ImageBrush ImageSource="{Binding ElementName=userCard,Path=Image}"/>
                </Ellipse.Fill>
            </Ellipse>

            <!--Info Text-->


            <StackPanel Grid.Column="1" Margin="5 0 0 0" >
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Product name: " Foreground="Black" FontSize="12" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding ProductName}" Foreground="Black" FontSize="12" FontWeight="SemiBold"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Price: " Foreground="Black" FontSize="10"/>
                    <TextBlock Text="{Binding ElementName=userCard,Path=Price}" Foreground="Black" FontSize="10" />
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Quantity: " Foreground="Black" FontSize="10" />
                    <TextBlock Text="{Binding ElementName=userCard,Path=Quantity}" Foreground="Black" FontSize="10" />
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Total Price: " Foreground="#039F41" FontSize="10"/>
                <TextBlock Text="{Binding ElementName=userCard,Path=TotalPrice}" Foreground="#039F41" FontSize="10"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
