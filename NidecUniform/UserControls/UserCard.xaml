<UserControl x:Class="NidecUniform.UserControls.UserCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             Name="userCard">
    <Border Margin="10 0 10 5">
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="CornerRadius" Value="10" />
                <Setter Property="BorderThickness" Value="2"/>
                <Setter Property="Margin" Value="0 4"/>
                <Setter Property="BorderBrush" Value="#039F41"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#9DC08B"/>
                    </Trigger>
                    <DataTrigger Binding="{Binding ElementName=userCard,Path=IsActive}">
                        <Setter Property="Background" Value="#f3f3f3"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <!--User Image-->
            <Rectangle  Width="50" Height="50"  VerticalAlignment="Center" HorizontalAlignment="Center">
                <Rectangle.Fill>
                    <ImageBrush ImageSource="{Binding ElementName=userCard,Path=Image}" Stretch="Uniform"/>
                </Rectangle.Fill>
            </Rectangle>

            <!--Info Text-->
            <StackPanel Grid.Column="1" Margin="5 0 0 0" >
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="P.Name: " Foreground="Black" FontSize="15" FontWeight="SemiBold" Grid.Column="0"/>
                    <TextBlock Text="{Binding ProductName}" Foreground="Black" FontSize="15" TextWrapping="Wrap" FontWeight="SemiBold" Grid.Column="1"/>
                </Grid>
                <StackPanel Orientation="Horizontal">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Price: " Foreground="Black" FontSize="15"/>
                        <TextBlock Text="{Binding Price}" Foreground="Black" FontSize="15" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="30 0 0 0">
                        <TextBlock Text="Quantity: " Foreground="Black" FontSize="15" />
                        <TextBlock Text="{Binding Quantity}" Foreground="Black" FontSize="15" />
                    </StackPanel>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Total Price:"  FontSize="15" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding TotalPrice}" FontSize="15" FontWeight="SemiBold" Margin="10 0 0 0"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
