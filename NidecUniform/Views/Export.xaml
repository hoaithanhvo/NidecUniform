<UserControl x:Class="NidecUniform.Views.Export"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             xmlns:vm="clr-namespace:NidecUniform.ViewModels"
             xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <!--<Border CornerRadius="0,40,40,0" Margin="0 5,5,0" Background="White">-->
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <Border CornerRadius="10" Padding="5" Background="#039F41"  HorizontalAlignment="Left" Width="130" Margin="5">
                <TextBlock HorizontalAlignment="Center" Text="Export | Log"  Style="{StaticResource mainTitle}"/>
            </Border>
        </StackPanel>
        <StackPanel VerticalAlignment="Center" Grid.Row="1" Orientation="Horizontal" Margin="5 0">
            <Icon:PackIconMaterial Kind="CalendarExport" Width="30" Height="30" Background="#039F41" />
            <Icon:PackIconMaterial Kind="Autorenew" Width="30" Height="30" Background="#039F41" />
        </StackPanel>
        <Grid VerticalAlignment="Top" Grid.Row="2" >
            <Border BorderBrush="#039F41" Margin="5"  BorderThickness="2" CornerRadius="10" >
                <DataGrid ItemsSource="{Binding DgvExportRequest}" GridLinesVisibility="Horizontal"  Margin="5"
       Background="White" BorderBrush="White" FontSize="12" FontStretch="SemiCondensed" AutoGenerateColumns="True" FontWeight="SemiBold"
       IsReadOnly="True">
                    <!--<DataGrid.Columns>
                        <DataGridTextColumn Header="#" Width="25" Binding="{Binding No}" />
                        <DataGridTextColumn Header="Emp. ID" Width="60" Binding="{Binding EmployeeID}" />
                        <DataGridTextColumn Header="Full Name" Width="170" Binding="{Binding FullName}" />
                        <DataGridTextColumn Header="Department" Width="80" Binding="{Binding Dept}" />
                        <DataGridTextColumn Header="Section" Width="150" Binding="{Binding Dept}" />
                        <DataGridTextColumn Header="U.Type"  Width="120" Binding="{Binding UniformType}" />
                        <DataGridTextColumn Header="Product Type" Width="150" Binding="{Binding ProductType}" />
                        <DataGridTextColumn Header="P.Qty" Width="70"  Binding="{Binding ProductQty}" />
                        <DataGridTextColumn Header="DateTime"  Width="180" Binding="{Binding EndDate, StringFormat=d}" />
                    </DataGrid.Columns>-->
                </DataGrid>
            </Border>
        </Grid>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="20 0">
            <TextBlock Text="Total:" VerticalAlignment="Center" FontSize="13" Foreground="Black"/>
            <TextBlock Text="{Binding TotalCount,TargetNullValue = 0}" VerticalAlignment="Center" FontSize="13" Foreground="Black" Margin="2 0 10 0"/>
            <!--<Button  Content="Export" Command="{Binding ExportCommand}"  Style="{StaticResource mainButton}" >
            </Button>-->
        </StackPanel>
    </Grid>
</UserControl>
