<UserControl x:Class="NidecUniform.Views.Scan"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             xmlns:vm="clr-namespace:NidecUniform.ViewModels"
             xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             d:DesignHeight="760" d:DesignWidth="1280">
    <!--<UserControl.DataContext>
        <vm:ScanVM />
    </UserControl.DataContext>-->
    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="White" Offset="0.0"/>
                <GradientStop Color="#9df6b0" Offset="0.5"/>
                <GradientStop Color="#039F41" Offset="0.5"/>
            </LinearGradientBrush>
        </Grid.Background>
        <Border CornerRadius="0,40,40,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="70"/>
                    <RowDefinition Height="130"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="70"/>
                </Grid.RowDefinitions>
                <StackPanel Margin="10 0" Orientation="Horizontal">
                    <TextBox x:Name="txtSearch"  Style="{StaticResource searchTextbox}"  Height="40" Width="395"  HorizontalAlignment="Left" Text="{Binding BDSearch}"/>
                    <Button Width="100" Height="40" FontSize="20" Content="Find" Style="{StaticResource mainButton}" Command="{Binding SearchCommand,Mode=TwoWay}"/>
                </StackPanel>
                <Grid Grid.Row="1" HorizontalAlignment="Left" VerticalAlignment="Top">
                    <Border Background="#039F41" BorderThickness="2" CornerRadius="10" Padding="10" Margin="10 0" Height="130" Width="500">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="150"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <!-- Employee ID -->
                                <StackPanel Orientation="Horizontal">
                                    <Label Width="100" Margin="10 0"  Content="EmployeeID"
                                           Style="{StaticResource mainLabel}"/>
                                    <Label  Content="{Binding BDEmployeeID}" Style="{StaticResource mainLabel}" />
                                </StackPanel>

                                <!-- Full Name -->
                                <StackPanel Orientation="Horizontal">
                                    <Label Width="100" Margin="10 0" Content="FullName:" Style="{StaticResource mainLabel}"/>
                                    <Label Content="{Binding BDFullName}" Style="{StaticResource mainLabel}" />
                                </StackPanel>

                                <!-- Department -->
                                <StackPanel Orientation="Horizontal">
                                    <Label Width="100" Margin="10 0" Content="Department:" Style="{StaticResource mainLabel}"/>
                                    <Label Content="{Binding BDDepartment}"  Style="{StaticResource mainLabel}" />
                                </StackPanel>

                                <!-- Section -->
                                <StackPanel Orientation="Horizontal">
                                    <Label Width="100" Margin="10 0" Content="Section:" Style="{StaticResource mainLabel}"/>
                                    <Label Content="{Binding BDSection}"  Style="{StaticResource mainLabel}" />
                                </StackPanel>
                            </StackPanel>
                            <StackPanel Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Bottom">
                                <Icon:PackIconMaterial Kind="Account" Width="100" Height="100" Background="#039F41" />
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>
                <Grid Grid.Row="2" >
                    <ListView ItemsSource="{Binding BDListRequest}" BorderBrush="#039F41" Margin="10" BorderThickness="2">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="Background" Value="White"/>
                                <Setter Property="FontSize" Value="15"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Foreground" Value="Black"/>
                                <Style.Triggers>
                                    <!-- Đổi màu khi rê chuột -->
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#D3D3D3"/>
                                    </Trigger>
                                    <!-- Đổi màu khi được chọn -->
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#039F41"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.View>
                            <GridView >
                                <GridViewColumn Header="#" Width="50">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <CheckBox IsChecked="{Binding CheckBox, Mode=TwoWay}" />
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Product Name" DisplayMemberBinding="{Binding ProductName}" Width="300"/>
                                <GridViewColumn Header="Start Date" Width="150">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Margin="30 0 0 0" Text="{Binding StartDate}"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="End Date" Width="150">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Margin="30 0 0 0" Text="{Binding EndDate}"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Requested" Width="100">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Margin="40 0" Text="{Binding QuantityRequested}" Width="40"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Delivered"  Width="100">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Margin="40 0" Text="{Binding QuantityDelivered}" Width="40"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Quantity" Width="150">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                <!-- Dấu trừ -->
                                                <Button Content="-" Width="30" Command="{Binding DecreaseCommand}" Margin="10 0 20 0" />

                                                <!-- Số lượng -->
                                                <TextBlock Text="{Binding QuantityRequested}" Width="40" HorizontalAlignment="Right" VerticalAlignment="Bottom"/>

                                                <!-- Dấu cộng -->
                                                <Button Content="+" Width="30" Command="{Binding IncreaseCommand}" />
                                            </StackPanel>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </Grid>
                <Grid Grid.Row="3" HorizontalAlignment="Right" Margin="10 0">
                    <Button  Content="Push" Command="{Binding PushCommand}" Width="100" Style="{StaticResource mainButton}" />
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
