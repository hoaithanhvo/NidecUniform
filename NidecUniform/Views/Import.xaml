<UserControl x:Class="NidecUniform.Views.Import"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             xmlns:vm="clr-namespace:NidecUniform.ViewModels"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:ImportVM />
    </UserControl.DataContext>
    <Grid Background="Aqua">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel>
            <Button Width="50" Height="50" Content="Import" Command="{Binding ImportCommand}" />
        </StackPanel>
        <StackPanel VerticalAlignment="Bottom" Grid.Row="1">
            <DataGrid Height="400" x:Name="dtgExcel" ItemsSource="{Binding Items}">
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>
